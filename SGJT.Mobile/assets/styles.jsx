import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        margin: 16
    },
    bottomSpacer: {
        marginBottom: 16
    },
    input: {
        color: "#000"
    },
    label: {
        fontWeight: "normal",
        fontSize: 14,
        color: "#000"
    }
});

export default styles;